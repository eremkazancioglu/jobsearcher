# Get credentials at https://developer.adzuna.com/
ADZUNA_APP_ID=your_app_id_here
ADZUNA_APP_KEY=your_app_key_here

# Two-letter country code Adzuna uses in its endpoint path, e.g. gb, us, de
ADZUNA_COUNTRY=gb

# Used by the Claude Agent SDK for capture confirmation/extraction and the
# tier 3 fallback web search (agents/fetchers.py)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Neon Postgres connection string -- point local runs at a `dev` branch,
# not `main` (see CLAUDE.md's Environments section)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Path to your resume (plain text or markdown). Keep it under personal/ --
# that whole folder is gitignored since this is personal data.
RESUME_PATH=personal/your_resume.md

# Path to a free-prose markdown file of job preferences (role/seniority,
# location/remote stance, comp floor, industries to avoid, deal-breakers --
# anything that isn't a qualification but still determines fit). Same
# treatment as RESUME_PATH: keep it under personal/, which is gitignored.
PREFERENCES_PATH=personal/your_preferences.md

# Slack incoming webhook for digest/send_digest.py. Optional -- if unset,
# the digest prints to stdout instead of sending to Slack.
SLACK_WEBHOOK_URL=

# Langfuse (cloud.langfuse.com) -- per-LLM-call tracing. Optional -- if
# LANGFUSE_PUBLIC_KEY is unset, tracing silently no-ops (same graceful
# fallback as SLACK_WEBHOOK_URL above). Langfuse Cloud has separate
# regions with different base URLs (EU: https://cloud.langfuse.com, US:
# https://us.cloud.langfuse.com) -- get keys and the correct base URL
# from your project's Settings -> API Keys page; using the wrong region's
# URL fails auth even with correct keys.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com
